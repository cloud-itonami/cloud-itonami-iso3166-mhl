{:itonami.blueprint/id "cloud-itonami-iso3166-mhl"
 :itonami.blueprint/name "Independent Public-Sector Market-Entry & Procurement Compliance Service — Marshall Islands"
 :itonami.blueprint/iso3166 "MHL"
 :itonami.blueprint/domain :public-sector/market-entry-compliance
 :itonami.blueprint/license "AGPL-3.0-or-later"
 :itonami.blueprint/social-impact [:sme-market-access :public-spend-transparency :cross-border-friction-reduction]
 :itonami.blueprint/governor :market-entry-compliance-governor
 :itonami.blueprint/robotics false
 :itonami.blueprint/required-technologies [:identity :forms :dmn :bpmn :audit-ledger]
 :itonami.blueprint/status :public-oss
 :itonami.blueprint/maturity :implemented
 :itonami.blueprint/implemented-slice
 "src/marketentry/* (langgraph-clj StateGraph actor, compiled via
 `operation/build` -> `g/state-graph` -> `g/compile-graph` with
 `interrupt-before #{:request-approval}`): facts.cljc (official
 spec-basis catalog encoding MHL's dual-track registration structure
 -- IRI-administered offshore RMI Corporate/Ships Registries vs.
 Ministry-of-Finance domestic business license -- plus FIBL, current
 vs. repealed procurement legal basis, and a verified COFA
 no-bidding-preference negative), governor.cljc (Market-Entry
 Compliance Governor -- spec-basis HARD check, evidence-incomplete
 HARD check, `:domestic-track-missing` HARD check independently
 verifying `:has-domestic-license?` when `:bids-domestically?` is
 true (an offshore-only IRI registration does NOT satisfy this) --
 the flagship new check for this vertical --, `:fibl-missing` HARD
 check (Foreign Investment Business License + structural National-
 Reserved-List-awareness gate, no sector list enumerated),
 `:procurement-basis-stale` HARD check (rejects citation of the
 repealed 1988 Procurement Code instead of the current Procurement
 Code Act, 2023 -- P.L. 2023-62), `:gross-revenue-tax-unacknowledged`
 HARD check (no specific rate figure ever asserted),
 `:cofa-preference-fabricated` HARD check (rejects any claimed COFA
 procurement-bidding preference regardless of bidder nationality --
 encodes a verified negative rather than omitting the topic),
 engagement-fee-mismatch independent recompute, confidence-floor/
 actuation gate, plus double-draft/double-submit guards), store.cljc
 (MemStore + DatomicStore via `io.github.kotoba-lang/langchain-store`
 (`ls/enc`/`ls/dec*`/`ls/read-stream`/`ls/append-blob!`) instead of a
 hand-rolled EDN-blob codec, 8-engagement demo fixture set -- one
 clean baseline plus one fixture per new check), registry.cljc
 (pure-function filing-draft/filing-submit record construction + fee
 recompute), marketentryllm.cljc (mock MarketEntry Advisor),
 operation.cljc (intake -> advise -> govern -> decide ->
 [request-approval ->] commit/hold StateGraph, `store/append-ledger!`
 called from both the `:commit` and `:hold` nodes), phase.cljc (0->3
 rollout, `:filing/draft`/`:filing/submit` permanently excluded from
 every phase's `:auto` set), sim.cljc (demo driver walking all five
 new-check HOLD scenarios plus the clean happy path). 36 tests / 120
 assertions passing (governor contract + store parity + facts + phase
 + registry, plus this repo's pre-existing separate `culture` domain
 namespace, left untouched), clj-kondo clean (0 errors, 9 non-fatal
 unused-binding/unused-require warnings, all matching the same
 pre-existing pattern in the AGO reference implementation)."}
